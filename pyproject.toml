[project]
name = "br-financial-ai"
version = "0.1.0"
description = "AI Engineering study for Brazilian financial-market analysis"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.19.1",
    "fastapi>=0.141.1",
    "langchain>=1.3.16",
    "langchain-core>=1.6.0",
    "langchain-ollama>=1.1.0",
    "langchain-postgres>=0.0.17",
    "pgvector>=0.3.6,<0.4",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.15.0",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.52",
    "sqlmodel>=0.0.39",
    "uvicorn[standard]>=0.52.4",
    "yfinance>=1.7.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "httpx2>=2.12.0",
    "pytest>=9.1,<10",
    "pytest-asyncio>=1.4.0",
    "pytest-bdd>=8.1.0",
    "ruff>=0.16.4",
]
[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-ra"
asyncio_mode = "strict"
markers = [
    "eval: external evaluation suite",
    "slow: slow evaluation",
    "external: requires network or local providers",
    "ollama: requires local Ollama",
    "yahoo: requires Yahoo Finance",
]

[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "UP",
    "B",
    "ASYNC",
]


[project.scripts]
br-financial-ai = "br_financial_ai.cli:main"
